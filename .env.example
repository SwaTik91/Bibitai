# Copy to .env. Never commit real keys.
# Spot keys only. Disable withdrawals. Use IP whitelist.

BINANCE_API_KEY=
BINANCE_API_SECRET=

# Required together with --i-understand-risk to place live orders.
# BIBITAI_LIVE=YES
